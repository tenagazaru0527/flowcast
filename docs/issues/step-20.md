# Step 20: ゲーム層に再生と説明を追加する

Issue #36の実装記録。

## スコープ

- 変更対象は`game/`と`docs/`のみ。
- `src/`、`scripts/`、`test/`、`runtime-hashes.json`、`debug/`、engineVersion、steps、標本数は変更しない。
- Canvas 2Dのみを使用し、外部ライブラリ、Web Worker、演出、段階的誘導は追加しない。

## 実装要件

1. Step 18のクリア解（中央線終点y=24、`stateHash=9e564fe2`）をJSONとして保存し、報告を訂正する。
2. `sampleDensity: true`で得た36標本を共通色スケールで自動再生し、現在step、再生ボタン、任意標本へ移動するスライダーを表示する。
3. 同期実行前に計算中表示を反映する。
4. 課題の目標・制約・現在値をchallenge定義から表示し、初期線が失敗見本であることと操作方法を表示する。
5. シンク群を色分けし、群名と到達量を表示する。

## 検証要件

- 元の線でupper 69.17%、場外0%、到達率67%、失敗。
- クリア解で`stateHash=9e564fe2`、クリア。
- 36標本、共通色スケール、再生後の最終表示、スライダー、再生ボタンを確認する。
- `npm run verify`と保護対象差分の確認を行う。
